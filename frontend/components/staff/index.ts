// The staff-console UI kit: presentational, CUBIK-native (uses the `.shell` tokens and
// `u-*` classes), shared by the teacher (/admin) and researcher (/researcher) surfaces.
// No new global CSS — every component styles with Tailwind + existing classes + tokens.
export { StatCard, StatGrid } from "./StatCard"
export { Panel } from "./Panel"
export { DataTable, THEAD_ROW_STYLE, TROW_STYLE } from "./DataTable"
export { Banner } from "./Banner"
export { EmptyState } from "./EmptyState"
export { Skeleton, ConsoleSkeleton } from "./Skeleton"
export { StaffHeader } from "./StaffHeader"
